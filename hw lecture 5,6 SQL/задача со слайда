ALTER TABLE tablitsa ADD summa_strok VARCHAR(50) DEFAULT (select count(*) from tablitsa);
К сожалению нет возможности попробовать в действии данное решение задачи, ибо у меня МАК, а в представленных тренажёрах в интернете нельзя модифицировать таблицы.
Среди оконных функций не нашел ничего подходящего. Максимум row_number() over() более-менее приближается к решению задачи. 
